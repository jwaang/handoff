configVersion = "2.1"
defaultAgent = "claude"
defaultTracker = "json"
maxIterations = 10
iterationDelay = 0
agent = "claude"
tracker = "json"
autoCommit = true
subagentTracingDetail = "off"

[notifications]
sound = "system"

[parallel]
mode = "auto"
maxWorkers = 1
directMerge = true
